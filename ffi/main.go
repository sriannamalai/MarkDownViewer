// Command ffi builds libmdviewer, the C-shared FFI boundary for the
// markdownviewer library. Build with:
//
//	go build -buildmode=c-shared -o libmdviewer.<ext> ./ffi
//
// See scripts/build-ffi.sh.
package main

func main() {}
